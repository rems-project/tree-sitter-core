-- Globals
glob glob: pointer [ail_ctype = 'char'] :=
  let strong a_516: pointer = create(Ivalignof('char'), 'char') in
  let strong a_517: loaded integer =
    {- §6.5#2 -}
    bound(pure(Specified(10))) in
  store('char', a_516, conv_loaded_int('char', a_517)) ;
  pure(a_516)

-- Fun map
proc main (): eff loaded integer :=
  let strong p: pointer = create(Ivalignof('char*'), 'char*') in
  let strong a_507: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.2#3, sentence 5 -}
      let weak a_508: pointer = pure(glob) in
      pure(Specified(a_508))
    ) in
  store('char*', p, a_507) ;
  let strong a_514: loaded integer =
    {- §6.5#2 -}
    bound(
      let weak a_513: pointer =
        {- §6.5.3.2 -}
        let weak a_509: loaded pointer =
          let weak a_512: pointer = pure(p) in
          load('char*', a_512) in
        let Specified(a_510: pointer) = a_509 in
        let weak a_511: boolean = memop(PtrValidForDeref, 'char', a_510) in
        pure(
          if a_511 then a_510 else undef(<<UB043_indirection_invalid_value>>)
        ) in
      load('char', a_513)
    ) in
  kill('char*', p) ;
  run ret_506(conv_loaded_int('signed int', a_514)) ;
  kill('char*', p) ;
  pure(Unit) ;
  save ret_506: loaded integer (a_515: loaded integer:= Specified(0)) in
    pure(a_515)

