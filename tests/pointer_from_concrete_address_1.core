proc main (): eff loaded integer :=
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_505: pointer, a_512: loaded integer) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.3.2 -}
          let weak a_506: loaded pointer =
            {- §6.5.4 -}
            let weak a_509: loaded integer = pure(Specified(2748)) in
            let Specified(a_510: integer) = a_509 in
            let weak a_511: pointer =
              memop(PtrFromInt, 'signed int', 'signed int', a_510) in
            pure(Specified(a_511)) in
          let Specified(a_507: pointer) = a_506 in
          let weak a_508: boolean =
            memop(PtrValidForDeref, 'signed int', a_507) in
          pure(
            if a_508 then
              a_507
            else
              undef(<<UB043_indirection_invalid_value>>)
          )
        ,
          pure(Specified(123))
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('signed int', a_505, conv_loaded_int('signed int', a_512))) in
      pure(conv_loaded_int('signed int', a_512))
    ) in
  pure(Unit) ;
  pure(Unit) ;
  save ret_504: loaded integer (a_513: loaded integer:= Specified(0)) in
    pure(a_513)

