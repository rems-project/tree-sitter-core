proc f (): eff loaded integer :=
  let strong i: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong j: pointer = create(Ivalignof('signed int'), 'signed int') in
  {- §6.2.4#6 -}
  store('signed int', i, Unspecified('signed int')) ;
  {- §6.2.4#6 -}
  store('signed int', j, Unspecified('signed int')) ;
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_507: pointer, a_509: loaded integer) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          pure(j)
        ,
          let weak a_508: pointer = pure(i) in
          load('signed int', a_508)
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('signed int', a_507, conv_loaded_int('signed int', a_509))) in
      pure(conv_loaded_int('signed int', a_509))
    ) in
  pure(Unit) ;
  let strong a_511: loaded integer =
    {- §6.5#2 -}
    bound(
      let weak a_510: pointer = pure(j) in
      load('signed int', a_510)
    ) in
  kill('signed int', i) ;
  kill('signed int', j) ;
  run ret_506(conv_loaded_int('signed int', a_511)) ;
  kill('signed int', i) ;
  kill('signed int', j) ;
  pure(Unit) ;
  save ret_506: loaded integer (a_512: loaded integer:= undef(<<UB088_reached_end_of_function>>)) in
    pure(a_512)

