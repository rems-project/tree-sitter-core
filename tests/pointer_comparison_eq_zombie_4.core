-- Aggregates
def struct div :=
  quot: 'signed int'
  rem: 'signed int'

def struct ldiv :=
  quot: 'signed long'
  rem: 'signed long'

def struct lldiv :=
  quot: 'signed long_long'
  rem: 'signed long_long'

def union _G_fpos64_t :=
  __opaque: 'char[16]'
  __lldata: 'signed long_long'
  __align: 'double'

def struct s :=
  p: 'signed int*'

-- Globals
-- Fun map
proc main (): eff loaded integer :=
  let strong a_826: pointer = create(Ivalignof('struct s'), 'struct s') in
  let strong a_816: pointer = create(Ivalignof('struct s'), 'struct s') in
  let strong x: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a: pointer = create(Ivalignof('void*'), 'void*') in
  let strong p0: pointer = create(Ivalignof('struct s*'), 'struct s*') in
  let strong p1: pointer = create(Ivalignof('struct s*'), 'struct s*') in
  let strong q: pointer = create(Ivalignof('signed int*'), 'signed int*') in
  let strong a_758: loaded integer =
    {- §6.5#2 -}
    bound(pure(Specified(0))) in
  store('signed int', x, conv_loaded_int('signed int', a_758)) ;
  let strong a_759: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.2.2#10, sentence 1 -}
      let strong ((a_761: loaded pointer,
      (a_762: ctype, a_763: [ctype], a_764: boolean, a_765: boolean)),
      a_767: loaded integer) =
        {- §6.5.2.2#4, sentence 2 -}
        unseq(
          let strong a_760: loaded pointer =
            pure(Specified(Cfunction(malloc_proxy))) in
          pure((a_760, cfunction(a_760)))
        ,
          {- §6.5.5 -}
          let weak (a_768: loaded integer, a_769: loaded integer) =
            unseq(pure(Specified(2)), pure(Specified(Ivsizeof('struct s')))) in
          pure(
            let (Specified(a_770: integer), Specified(a_771: integer)) =
              (a_768, a_769) in
            Specified(wrapI('size_t',
            if all_values_representable_in('size_t', 'signed int') then
              conv_int('signed int', a_770)
            else
              conv_int('unsigned int', a_770) *
            if all_values_representable_in('size_t', 'signed int') then
              conv_int('signed int', a_771)
            else
              conv_int('unsigned int', a_771)))
          )
        ) in
      if not(params_length(a_763) = 1) then
        pure(undef(<<UB038_number_of_args>>))
      else
        if a_764 \/ not(are_compatible ('void*', a_762)) then
          pure(undef(<<UB041_function_not_compatible>>))
        else
          let strong a_766: pointer =
            let a_773: ctype = params_nth(a_763, 0) in
            if not(are_compatible ('size_t', a_773)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              {- §6.5.2.2#7, sentence 1 -}
              let weak a_774: pointer = create(Ivalignof(a_773), a_773) in
              let weak _: unit =
                store(a_773, a_774, conv_loaded_int(a_773, a_767)) in
              pure(a_774) in
          let strong a_775: loaded pointer =
            ccall('void* (*) (size_t)', a_761, a_766) in
          kill('size_t', a_766) ;
          pure(a_775)
    ) in
  store('void*', a, a_759) ;
  let strong a_776: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_777: loaded pointer =
        {- §6.5.6 -}
        let weak (a_779: loaded pointer, a_781: loaded integer) =
          unseq(
            {- §6.5.4 -}
            let weak a_784: loaded pointer =
              let weak a_786: pointer = pure(a) in
              load('void*', a_786) in
            let Specified(a_785: pointer) = a_784 in
            let weak a_787: boolean = memop(PtrWellAligned, 'char', a_785) in
            pure(
              Specified(if a_787 then
                a_785
              else
                undef(<<UB025_misaligned_pointer_conversion>>))
            )
          ,
            {- §6.5.5 -}
            let weak (a_788: loaded integer, a_789: loaded integer) =
              unseq(pure(Specified(0)), pure(Specified(Ivsizeof('struct s')))) in
            pure(
              let (Specified(a_790: integer), Specified(a_791: integer)) =
                (a_788, a_789) in
              Specified(wrapI('size_t',
              if all_values_representable_in('size_t', 'signed int') then
                conv_int('signed int', a_790)
              else
                conv_int('unsigned int', a_790) *
              if all_values_representable_in('size_t', 'signed int') then
                conv_int('signed int', a_791)
              else
                conv_int('unsigned int', a_791)))
            )
          ) in
        let (Specified(a_780: pointer), Specified(a_782: integer)) =
          (a_779, a_781) in
        let weak a_783: pointer =
          memop(PtrArrayShift, a_780, 'char', a_782) in
        pure(Specified(a_783)) in
      let Specified(a_778: pointer) = a_777 in
      let weak a_793: boolean = memop(PtrWellAligned, 'struct s', a_778) in
      pure(
        Specified(if a_793 then
          a_778
        else
          undef(<<UB025_misaligned_pointer_conversion>>))
      )
    ) in
  store('struct s*', p0, a_776) ;
  let strong a_794: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_795: loaded pointer =
        {- §6.5.6 -}
        let weak (a_797: loaded pointer, a_799: loaded integer) =
          unseq(
            {- §6.5.4 -}
            let weak a_802: loaded pointer =
              let weak a_804: pointer = pure(a) in
              load('void*', a_804) in
            let Specified(a_803: pointer) = a_802 in
            let weak a_805: boolean = memop(PtrWellAligned, 'char', a_803) in
            pure(
              Specified(if a_805 then
                a_803
              else
                undef(<<UB025_misaligned_pointer_conversion>>))
            )
          ,
            {- §6.5.5 -}
            let weak (a_806: loaded integer, a_807: loaded integer) =
              unseq(pure(Specified(1)), pure(Specified(Ivsizeof('struct s')))) in
            pure(
              let (Specified(a_808: integer), Specified(a_809: integer)) =
                (a_806, a_807) in
              Specified(wrapI('size_t',
              if all_values_representable_in('size_t', 'signed int') then
                conv_int('signed int', a_808)
              else
                conv_int('unsigned int', a_808) *
              if all_values_representable_in('size_t', 'signed int') then
                conv_int('signed int', a_809)
              else
                conv_int('unsigned int', a_809)))
            )
          ) in
        let (Specified(a_798: pointer), Specified(a_800: integer)) =
          (a_797, a_799) in
        let weak a_801: pointer =
          memop(PtrArrayShift, a_798, 'char', a_800) in
        pure(Specified(a_801)) in
      let Specified(a_796: pointer) = a_795 in
      let weak a_811: boolean = memop(PtrWellAligned, 'struct s', a_796) in
      pure(
        Specified(if a_811 then
          a_796
        else
          undef(<<UB025_misaligned_pointer_conversion>>))
      )
    ) in
  store('struct s*', p1, a_794) ;
  let strong _: loaded struct s_745 =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_757: pointer, a_821: loaded struct s_745) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.3.2 -}
          let weak a_812: loaded pointer =
            let weak a_815: pointer = pure(p0) in
            load('struct s*', a_815) in
          let Specified(a_813: pointer) = a_812 in
          let weak a_814: boolean =
            memop(PtrValidForDeref, 'struct s', a_813) in
          pure(
            if a_814 then
              a_813
            else
              undef(<<UB043_indirection_invalid_value>>)
          )
        ,
          let weak a_820: pointer =
            let weak a_817: loaded struct s_745 =
              let weak a_819: loaded pointer =
                {- §6.5.3.2#3, sentence 5 -}
                let weak a_818: pointer = pure(x) in
                pure(Specified(a_818)) in
              pure(Specified((struct s){.p= a_819})) in
            let weak _: unit = store('struct s', a_816, a_817) in
            pure(a_816) in
          load('struct s', a_820)
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('struct s', a_757, a_821)) in
      pure(a_821)
    ) in
  pure(Unit) ;
  let strong _: loaded struct s_745 =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_756: pointer, a_831: loaded struct s_745) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.3.2 -}
          let weak a_822: loaded pointer =
            let weak a_825: pointer = pure(p1) in
            load('struct s*', a_825) in
          let Specified(a_823: pointer) = a_822 in
          let weak a_824: boolean =
            memop(PtrValidForDeref, 'struct s', a_823) in
          pure(
            if a_824 then
              a_823
            else
              undef(<<UB043_indirection_invalid_value>>)
          )
        ,
          let weak a_830: pointer =
            let weak a_827: loaded struct s_745 =
              let weak a_829: loaded pointer =
                {- §6.5.3.2#3, sentence 5 -}
                let weak a_828: pointer = pure(x) in
                pure(Specified(a_828)) in
              pure(Specified((struct s){.p= a_829})) in
            let weak _: unit = store('struct s', a_826, a_827) in
            pure(a_826) in
          load('struct s', a_830)
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('struct s', a_756, a_831)) in
      pure(a_831)
    ) in
  pure(Unit) ;
  let strong a_832: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_833: loaded pointer =
        let weak a_835: pointer = pure(a) in
        load('void*', a_835) in
      let Specified(a_834: pointer) = a_833 in
      let weak a_836: boolean = memop(PtrWellAligned, 'signed int', a_834) in
      pure(
        Specified(if a_836 then
          a_834
        else
          undef(<<UB025_misaligned_pointer_conversion>>))
      )
    ) in
  store('signed int*', q, a_832) ;
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_755: pointer, a_841: loaded integer) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.3.2 -}
          let weak a_837: loaded pointer =
            let weak a_840: pointer = pure(q) in
            load('signed int*', a_840) in
          let Specified(a_838: pointer) = a_837 in
          let weak a_839: boolean =
            memop(PtrValidForDeref, 'signed int', a_838) in
          pure(
            if a_839 then
              a_838
            else
              undef(<<UB043_indirection_invalid_value>>)
          )
        ,
          pure(Specified(7))
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('signed int', a_755, conv_loaded_int('signed int', a_841))) in
      pure(conv_loaded_int('signed int', a_841))
    ) in
  pure(Unit) ;
  let strong a_842: loaded integer =
    {- §6.5#2 -}
    bound(
      let weak (a_844: loaded integer, a_845: loaded integer) =
        unseq(
          let weak (a_849: loaded pointer, a_850: loaded pointer) =
            unseq(
              let weak a_854: pointer = pure(p0) in
              load('struct s*', a_854)
            ,
              let weak a_855: pointer = pure(p1) in
              load('struct s*', a_855)
            ) in
          let (Specified(a_851: pointer), Specified(a_852: pointer)) =
            (a_849, a_850) in
          let weak a_853: boolean = memop(PtrEq, a_851, a_852) in
          pure(if a_853 then Specified(1) else Specified(0))
        ,
          pure(Specified(0))
        ) in
      pure(
        let (Specified(a_846: integer), Specified(a_847: integer)) =
          (a_844, a_845) in
        if conv_int('signed int', a_846) = conv_int('signed int', a_847) then
          Specified(1)
        else
          Specified(0)
      )
    ) in
  let strong a_754: boolean =
    let Specified(a_843: integer) = a_842 in
    pure(if not(a_843 = 1) then True else False) in
  if a_754 then
    let strong a_856: loaded integer =
      {- §6.5#2 -}
      bound(pure(Specified(1))) in
    kill('signed int', x) ;
    kill('void*', a) ;
    kill('struct s*', p0) ;
    kill('struct s*', p1) ;
    kill('signed int*', q) ;
    run ret_753(conv_loaded_int('signed int', a_856))
  else
    let strong a_857: loaded integer =
      {- §6.5#2 -}
      bound(pure(Specified(0))) in
    kill('signed int', x) ;
    kill('void*', a) ;
    kill('struct s*', p0) ;
    kill('struct s*', p1) ;
    kill('signed int*', q) ;
    run ret_753(conv_loaded_int('signed int', a_857)) ;
  kill('signed int', x) ;
  kill('void*', a) ;
  kill('struct s*', p0) ;
  kill('struct s*', p1) ;
  kill('signed int*', q) ;
  kill('struct s', a_826) ;
  kill('struct s', a_816) ;
  pure(Unit) ;
  save ret_753: loaded integer (a_858: loaded integer:= Specified(0)) in
    pure(a_858)

