-- Aggregates
def union _G_fpos64_t :=
  __opaque: 'char[16]'
  __lldata: 'signed long_long'
  __align: 'double'

def struct __cerbty_unnamed_tag_754 :=
  __dummy_max_align_t: 'void*'

def struct __cerbty_unnamed_tag_758 :=
  quot: 'intmax_t'
  rem: 'intmax_t'

-- Globals
glob y: pointer [ail_ctype = 'signed int'] :=
  let strong a_859: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_860: loaded integer =
    {- §6.5#2 -}
    bound(pure(Specified(2))) in
  store('signed int', a_859, conv_loaded_int('signed int', a_860)) ;
  pure(a_859)

glob x: pointer [ail_ctype = 'signed int'] :=
  let strong a_857: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_858: loaded integer =
    {- §6.5#2 -}
    bound(pure(Specified(1))) in
  store('signed int', a_857, conv_loaded_int('signed int', a_858)) ;
  pure(a_857)

-- Fun map
proc main (): eff loaded integer :=
  let strong p0: pointer = create(Ivalignof('signed int*'), 'signed int*') in
  let strong q0: pointer = create(Ivalignof('signed int*'), 'signed int*') in
  let strong i: pointer = create(Ivalignof('uintptr_t'), 'uintptr_t') in
  let strong j: pointer = create(Ivalignof('uintptr_t'), 'uintptr_t') in
  let strong p: pointer = create(Ivalignof('signed int*'), 'signed int*') in
  let strong q: pointer = create(Ivalignof('signed int*'), 'signed int*') in
  let strong a_791: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.6 -}
      let weak (a_792: loaded pointer, a_794: loaded integer) =
        unseq(
          {- §6.5.3.2#3, sentence 5 -}
          let weak a_797: pointer = pure(x) in
          pure(Specified(a_797))
        ,
          pure(Specified(1))
        ) in
      let (Specified(a_793: pointer), Specified(a_795: integer)) =
        (a_792, a_794) in
      let weak a_796: pointer =
        memop(PtrArrayShift, a_793, 'signed int', a_795) in
      pure(Specified(a_796))
    ) in
  store('signed int*', p0, a_791) ;
  let strong a_798: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.2#3, sentence 5 -}
      let weak a_799: pointer = pure(y) in
      pure(Specified(a_799))
    ) in
  store('signed int*', q0, a_798) ;
  let strong a_800: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_801: loaded pointer =
        let weak a_803: pointer = pure(p0) in
        load('signed int*', a_803) in
      let Specified(a_802: pointer) = a_801 in
      let weak a_804: integer =
        memop(IntFromPtr, 'signed int', 'uintptr_t', a_802) in
      pure(Specified(a_804))
    ) in
  store('uintptr_t', i, conv_loaded_int('uintptr_t', a_800)) ;
  let strong a_805: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_806: loaded pointer =
        let weak a_808: pointer = pure(q0) in
        load('signed int*', a_808) in
      let Specified(a_807: pointer) = a_806 in
      let weak a_809: integer =
        memop(IntFromPtr, 'signed int', 'uintptr_t', a_807) in
      pure(Specified(a_809))
    ) in
  store('uintptr_t', j, conv_loaded_int('uintptr_t', a_805)) ;
  let strong a_810: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_811: loaded integer =
        let weak a_813: pointer = pure(i) in
        load('uintptr_t', a_813) in
      let Specified(a_812: integer) = a_811 in
      let weak a_814: pointer =
        memop(PtrFromInt, 'uintptr_t', 'signed int', a_812) in
      pure(Specified(a_814))
    ) in
  store('signed int*', p, a_810) ;
  let strong a_815: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_816: loaded integer =
        let weak a_818: pointer = pure(j) in
        load('uintptr_t', a_818) in
      let Specified(a_817: integer) = a_816 in
      let weak a_819: pointer =
        memop(PtrFromInt, 'uintptr_t', 'signed int', a_817) in
      pure(Specified(a_819))
    ) in
  store('signed int*', q, a_815) ;
  let strong a_820: loaded integer =
    {- §6.5#2 -}
    bound(
      let weak (a_822: loaded integer, a_823: loaded integer) =
        unseq(
          let weak (a_827: loaded integer, a_828: loaded integer) =
            unseq(
              let weak a_832: pointer = pure(i) in
              load('uintptr_t', a_832)
            ,
              let weak a_833: pointer = pure(j) in
              load('uintptr_t', a_833)
            ) in
          pure(
            let (Specified(a_829: integer), Specified(a_830: integer)) =
              (a_827, a_828) in
            if conv_int('uintptr_t', a_829) = conv_int('uintptr_t', a_830) then
              Specified(1)
            else
              Specified(0)
          )
        ,
          pure(Specified(0))
        ) in
      pure(
        let (Specified(a_824: integer), Specified(a_825: integer)) =
          (a_822, a_823) in
        if conv_int('signed int', a_824) = conv_int('signed int', a_825) then
          Specified(1)
        else
          Specified(0)
      )
    ) in
  let strong a_790: boolean =
    let Specified(a_821: integer) = a_820 in
    pure(if not(a_821 = 1) then True else False) in
  if a_790 then
    let strong n: pointer = create(Ivalignof('ptrdiff_t'), 'ptrdiff_t') in
    let strong m: pointer = create(Ivalignof('ptrdiff_t'), 'ptrdiff_t') in
    let strong e: pointer = create(Ivalignof('signed int'), 'signed int') in
    let strong a_834: loaded integer =
      {- §6.5#2 -}
      bound(
        {- §6.5.6 -}
        let weak (a_837: loaded pointer, a_838: loaded pointer) =
          unseq(
            let weak a_835: pointer = pure(p) in
            load('signed int*', a_835)
          ,
            let weak a_836: pointer = pure(q) in
            load('signed int*', a_836)
          ) in
        let (Specified(a_839: pointer), Specified(a_840: pointer)) =
          (a_837, a_838) in
        let weak a_841: integer =
          memop(Ptrdiff, 'signed int', a_839, a_840) in
        pure(
          if is_representable_integer(a_841, 'ptrdiff_t') then
            Specified(a_841)
          else
            undef(<<UB050_pointers_subtraction_not_representable>>)
        )
      ) in
    store('ptrdiff_t', n, conv_loaded_int('ptrdiff_t', a_834)) ;
    let strong a_842: loaded integer =
      {- §6.5#2 -}
      bound(
        {- §6.5.6 -}
        let weak (a_845: loaded pointer, a_846: loaded pointer) =
          unseq(
            let weak a_843: pointer = pure(p) in
            load('signed int*', a_843)
          ,
            {- §6.5.3.2#3, sentence 5 -}
            let weak a_844: pointer = pure(x) in
            pure(Specified(a_844))
          ) in
        let (Specified(a_847: pointer), Specified(a_848: pointer)) =
          (a_845, a_846) in
        let weak a_849: integer =
          memop(Ptrdiff, 'signed int', a_847, a_848) in
        pure(
          if is_representable_integer(a_849, 'ptrdiff_t') then
            Specified(a_849)
          else
            undef(<<UB050_pointers_subtraction_not_representable>>)
        )
      ) in
    store('ptrdiff_t', m, conv_loaded_int('ptrdiff_t', a_842)) ;
    let strong a_850: loaded integer =
      {- §6.5#2 -}
      bound(
        let weak a_855: pointer =
          {- §6.5.3.2 -}
          let weak a_851: loaded pointer =
            let weak a_854: pointer = pure(q) in
            load('signed int*', a_854) in
          let Specified(a_852: pointer) = a_851 in
          let weak a_853: boolean =
            memop(PtrValidForDeref, 'signed int', a_852) in
          pure(
            if a_853 then
              a_852
            else
              undef(<<UB043_indirection_invalid_value>>)
          ) in
        load('signed int', a_855)
      ) in
    store('signed int', e, conv_loaded_int('signed int', a_850)) ;
    kill('ptrdiff_t', n) ;
    kill('ptrdiff_t', m) ;
    kill('signed int', e) ;
    pure(Unit)
  else
    pure(Unit) ;
  kill('signed int*', p0) ;
  kill('signed int*', q0) ;
  kill('uintptr_t', i) ;
  kill('uintptr_t', j) ;
  kill('signed int*', p) ;
  kill('signed int*', q) ;
  pure(Unit) ;
  save ret_789: loaded integer (a_856: loaded integer:= Specified(0)) in
    pure(a_856)

