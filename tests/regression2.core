proc main (): eff loaded integer :=
  let strong x: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_506: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.3#3 -}
      let weak a_508: loaded integer = pure(Specified(1)) in
      pure(
        let Specified(a_507: integer) = a_508 in
        Specified(catch_exceptional_condition('signed int',
        0 - conv_int('signed int', a_507)))
      )
    ) in
  store('signed int', x, conv_loaded_int('signed int', a_506)) ;
  let strong a_510: loaded integer =
    {- §6.5#2 -}
    bound(
      let weak a_509: pointer = pure(x) in
      load('signed int', a_509)
    ) in
  kill('signed int', x) ;
  run ret_505(conv_loaded_int('signed int', a_510)) ;
  kill('signed int', x) ;
  pure(Unit) ;
  save ret_505: loaded integer (a_511: loaded integer:= Specified(0)) in
    pure(a_511)

