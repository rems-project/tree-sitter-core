-- Aggregates
def union _G_fpos64_t :=
  __opaque: 'char[16]'
  __lldata: 'signed long_long'
  __align: 'double'

def struct __cerbty_unnamed_tag_646 :=
  i: 'signed int'
  f: 'float'

-- Globals
glob a_688: pointer [ail_ctype = 'char[6]'] :=
  create_readonly(Ivalignof('char[6]'), 'char[6]', Specified(Array(Specified(conv_int('char',
  102)), Specified(conv_int('char', 61)), Specified(conv_int('char', 37)),
  Specified(conv_int('char', 102)), Specified(conv_int('char', 10)),
  Specified(0))))

-- Fun map
proc f (pi: pointer): eff unit :=
  let strong pst: pointer =
    create(Ivalignof('struct __cerbty_unnamed_tag_646*'), 'struct __cerbty_unnamed_tag_646*') in
  let strong a_700: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_701: loaded pointer =
        let weak a_703: pointer = pure(pi) in
        load('signed int*', a_703) in
      let Specified(a_702: pointer) = a_701 in
      let weak a_704: boolean =
        memop(PtrWellAligned, 'struct __cerbty_unnamed_tag_646', a_702) in
      pure(
        Specified(if a_704 then
          a_702
        else
          undef(<<UB025_misaligned_pointer_conversion>>))
      )
    ) in
  store('struct __cerbty_unnamed_tag_646*', pst, a_700) ;
  let strong _: loaded floating =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_699: pointer, a_710: loaded floating) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.2.3#4, sentence 2 -}
          let strong a_705: loaded pointer =
            let weak a_709: pointer = pure(pst) in
            load('struct __cerbty_unnamed_tag_646*', a_709) in
          let Specified(a_706: pointer) = a_705 in
          let weak a_707: boolean =
            memop(PtrValidForDeref, 'struct __cerbty_unnamed_tag_646', a_706) in
          pure(
            if a_707 then
              member_shift(a_706, __cerbty_unnamed_tag_646, .f)
            else
              undef(<<UB043_indirection_invalid_value>>)
          )
        ,
          pure(Specified(2.))
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('float', a_699, a_710)) in
      pure(a_710)
    ) in
  pure(Unit) ;
  kill('struct __cerbty_unnamed_tag_646*', pst) ;
  pure(Unit) ;
  save ret_698: unit (a_711: unit:= Unit) in  pure(a_711)

proc main (): eff loaded integer :=
  let strong s: pointer =
    create(Ivalignof('struct __cerbty_unnamed_tag_646'), 'struct __cerbty_unnamed_tag_646') in
  let strong pi: pointer = create(Ivalignof('signed int*'), 'signed int*') in
  let strong f: pointer = create(Ivalignof('float'), 'float') in
  let strong a_660: loaded struct a_646 =
    {- §6.5#2 -}
    bound(
      let weak (a_662: loaded floating, a_661: loaded integer) =
        {- §6.7.9#23 -}
        unseq(pure(Specified(1.)), pure(Specified(1))) in
      pure(
        Specified((struct __cerbty_unnamed_tag_646){.i= conv_loaded_int('signed int',
        a_661), .f= a_662})
      )
    ) in
  store('struct __cerbty_unnamed_tag_646', s, a_660) ;
  let strong a_663: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.2#3, sentence 5 -}
      let weak a_664: pointer =
        {- §6.5.2.3#3, sentence 2 -}
        let strong a_665: pointer = pure(s) in
        memop(PtrArrayShift[__cerbty_unnamed_tag_646, i], a_665) in
      pure(Specified(a_664))
    ) in
  store('signed int*', pi, a_663) ;
  {- §6.5#2 -}
  bound(
    {- §6.5.2.2#10, sentence 1 -}
    let strong ((a_666: loaded pointer,
    (a_667: ctype, a_668: [ctype], a_669: boolean, a_670: boolean)),
    a_672: loaded pointer) =
      {- §6.5.2.2#4, sentence 2 -}
      unseq(
        let strong a_659: loaded pointer = pure(Specified(Cfunction(f))) in
        pure((a_659, cfunction(a_659)))
      ,
        let weak a_673: pointer = pure(pi) in
        load('signed int*', a_673)
      ) in
    if not(params_length(a_668) = 1) then
      pure(undef(<<UB038_number_of_args>>))
    else
      if a_669 \/ not(are_compatible ('void', a_667)) then
        pure(undef(<<UB041_function_not_compatible>>))
      else
        let strong a_671: pointer =
          let a_674: ctype = params_nth(a_668, 0) in
          if not(are_compatible ('signed int*', a_674)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            {- §6.5.2.2#7, sentence 1 -}
            let weak a_675: pointer = create(Ivalignof(a_674), a_674) in
            let weak _: unit = store(a_674, a_675, a_672) in
            pure(a_675) in
        let strong a_676: unit =
          ccall('void (*) (signed int*)', a_666, a_671) in
        kill('signed int*', a_671) ;
        pure(a_676)
  ) ;
  pure(Unit) ;
  let strong a_677: loaded floating =
    {- §6.5#2 -}
    bound(
      let weak a_679: pointer =
        {- §6.5.2.3#3, sentence 2 -}
        let strong a_678: pointer = pure(s) in
        memop(PtrArrayShift[__cerbty_unnamed_tag_646, f], a_678) in
      load('float', a_679)
    ) in
  store('float', f, a_677) ;
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.2.2#10, sentence 1 -}
      let strong ((a_680: loaded pointer,
      (a_681: ctype, a_682: [ctype], a_683: boolean, a_684: boolean)),
      a_687: loaded pointer, a_691: loaded floating) =
        {- §6.5.2.2#4, sentence 2 -}
        unseq(
          let strong a_658: loaded pointer =
            pure(Specified(Cfunction(printf_proxy))) in
          pure((a_658, cfunction(a_658)))
        ,
          let weak a_689: pointer = pure(a_688) in
          {- §6.3.2.1#3 -}
          let weak a_690: pointer = memop(PtrArrayShift, a_689, 'char', 0) in
          pure(Specified(a_690))
        ,
          let weak a_692: pointer = pure(f) in
          load('float', a_692)
        ) in
      if not(params_length(a_682) <= 2) then
        pure(undef(<<UB038_number_of_args>>))
      else
        if not(a_683) \/ not(are_compatible ('signed int', a_681)) then
          pure(undef(<<UB041_function_not_compatible>>))
        else
          let strong a_686: pointer =
            let a_693: ctype = params_nth(a_682, 0) in
            if not(are_compatible ('const char*', a_693)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              {- §6.5.2.2#7, sentence 1 -}
              let weak a_694: pointer = create(Ivalignof(a_693), a_693) in
              let weak _: unit = store(a_693, a_694, a_687) in
              pure(a_694) in
          let strong a_685: pointer =
            {- §6.5.2.2#7, sentences 2 and 3 -}
            pcall(create_and_store, 'double', a_691) in
          let strong a_695: loaded integer =
            ccall('signed int (*) (const char*restrict , ...)', a_680, a_686,
            [('double', a_685)]) in
          let strong _: (unit,unit) =
            unseq(kill('char*', a_686), kill('double', a_685)) in
          pure(a_695)
    ) in
  pure(Unit) ;
  kill('struct __cerbty_unnamed_tag_646', s) ;
  kill('signed int*', pi) ;
  kill('float', f) ;
  pure(Unit) ;
  save ret_657: loaded integer (a_696: loaded integer:= Specified(0)) in
    pure(a_696)

