-- Aggregates
def union _G_fpos64_t :=
  __opaque: 'char[16]'
  __lldata: 'signed long_long'
  __align: 'double'

-- Globals
glob a_660: pointer [ail_ctype = 'char[14]'] :=
  create_readonly(Ivalignof('char[14]'), 'char[14]', Specified(Array(Specified(conv_int('char',
  104)), Specified(conv_int('char', 101)), Specified(conv_int('char', 108)),
  Specified(conv_int('char', 108)), Specified(conv_int('char', 111)),
  Specified(conv_int('char', 32)), Specified(conv_int('char', 119)),
  Specified(conv_int('char', 111)), Specified(conv_int('char', 114)),
  Specified(conv_int('char', 108)), Specified(conv_int('char', 100)),
  Specified(conv_int('char', 33)), Specified(conv_int('char', 10)),
  Specified(0))))

-- Fun map
proc main (): eff loaded integer :=
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.2.2#10, sentence 1 -}
      let strong ((a_650: loaded pointer,
      (a_651: ctype, a_652: [ctype], a_653: boolean, a_654: boolean)),
      a_657: loaded pointer, a_659: loaded pointer) =
        {- §6.5.2.2#4, sentence 2 -}
        unseq(
          let strong a_649: loaded pointer =
            pure(Specified(Cfunction(fprintf))) in
          pure((a_649, cfunction(a_649)))
        ,
          let weak a_658: pointer = pure(__stderr) in
          load('struct _IO_FILE*', a_658)
        ,
          let weak a_661: pointer = pure(a_660) in
          {- §6.3.2.1#3 -}
          let weak a_662: pointer = memop(PtrArrayShift, a_661, 'char', 0) in
          pure(Specified(a_662))
        ) in
      if not(params_length(a_652) <= 2) then
        pure(undef(<<UB038_number_of_args>>))
      else
        if not(a_653) \/ not(are_compatible ('signed int', a_651)) then
          pure(undef(<<UB041_function_not_compatible>>))
        else
          let strong a_656: pointer =
            let a_663: ctype = params_nth(a_652, 0) in
            if not(are_compatible ('struct _IO_FILE*', a_663)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              {- §6.5.2.2#7, sentence 1 -}
              let weak a_664: pointer = create(Ivalignof(a_663), a_663) in
              let weak _: unit = store(a_663, a_664, a_657) in
              pure(a_664) in
          let strong a_655: pointer =
            let a_665: ctype = params_nth(a_652, 1) in
            if not(are_compatible ('const char*', a_665)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              {- §6.5.2.2#7, sentence 1 -}
              let weak a_666: pointer = create(Ivalignof(a_665), a_665) in
              let weak _: unit = store(a_665, a_666, a_659) in
              pure(a_666) in
          let strong a_667: loaded integer =
            ccall('signed int (*) (struct _IO_FILE*restrict , const char*restrict , ...)',
            a_650, a_656, a_655, []: (ctype,pointer)) in
          let strong _: (unit,unit) =
            unseq(kill('struct _IO_FILE*', a_656), kill('char*', a_655)) in
          pure(a_667)
    ) in
  pure(Unit) ;
  pure(Unit) ;
  save ret_648: loaded integer (a_668: loaded integer:= Specified(0)) in
    pure(a_668)

