proc main (): eff loaded integer :=
  let strong x: pointer = create(Ivalignof('signed int'), 'signed int') in
  {- §6.2.4#6 -}
  store('signed int', x, Unspecified('signed int')) ;
  let strong _: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_534: loaded integer =
        {- §6.5.6 -}
        let weak (a_536: loaded integer, a_537: loaded integer) =
          unseq(
            {- §6.5.4 -}
            let weak a_541: loaded pointer =
              {- §6.5.3.2#3, sentence 5 -}
              let weak a_543: pointer = pure(x) in
              pure(Specified(a_543)) in
            let Specified(a_542: pointer) = a_541 in
            let weak a_544: integer =
              memop(IntFromPtr, 'signed int', 'uintptr_t', a_542) in
            pure(Specified(a_544))
          ,
            pure(Specified(1))
          ) in
        pure(
          let (Specified(a_538: integer), Specified(a_539: integer)) =
            (a_536, a_537) in
          Specified(wrapI('uintptr_t',
          conv_int('uintptr_t', a_538) + conv_int('uintptr_t', a_539)))
        ) in
      let Specified(a_535: integer) = a_534 in
      let weak a_545: pointer =
        memop(PtrFromInt, 'uintptr_t', 'signed int', a_535) in
      pure(Specified(a_545))
    ) in
  pure(Unit) ;
  kill('signed int', x) ;
  pure(Unit) ;
  save ret_533: loaded integer (a_546: loaded integer:= Specified(0)) in
    pure(a_546)

