-- Aggregates
def struct div :=
  quot: 'signed int'
  rem: 'signed int'

def struct ldiv :=
  quot: 'signed long'
  rem: 'signed long'

def struct lldiv :=
  quot: 'signed long_long'
  rem: 'signed long_long'

def struct __cerbty_unnamed_tag_603 :=
  __dummy_max_align_t: 'void*'

def struct __cerbty_unnamed_tag_635 :=
  quot: 'intmax_t'
  rem: 'intmax_t'

def struct __cerbty_unnamed_tag_653 :=
  c: 'char'
  u: 'unsigned long_long'

-- Globals
glob xg: pointer [ail_ctype = 'unsigned long_long'] :=
  let strong a_765: pointer =
    create(Ivalignof('unsigned long long'), 'unsigned long long') in
  let strong a_766: loaded integer =
    {- §6.5#2 -}
    bound(pure(Specified(5))) in
  store('unsigned long long', a_765,
  conv_loaded_int('unsigned long long', a_766)) ;
  pure(a_765)

-- Fun map
proc f (pu: pointer): eff unit :=
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_758: pointer, a_763: loaded integer) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.3.2 -}
          let weak a_759: loaded pointer =
            let weak a_762: pointer = pure(pu) in
            load('unsigned long long*', a_762) in
          let Specified(a_760: pointer) = a_759 in
          let weak a_761: boolean =
            memop(PtrValidForDeref, 'unsigned long long', a_760) in
          pure(
            if a_761 then
              a_760
            else
              undef(<<UB043_indirection_invalid_value>>)
          )
        ,
          pure(Specified(9))
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('unsigned long long', a_758,
        conv_loaded_int('unsigned long long', a_763))) in
      pure(conv_loaded_int('unsigned long long', a_763))
    ) in
  pure(Unit) ;
  pure(Unit) ;
  save ret_757: unit (a_764: unit:= Unit) in  pure(a_764)

proc main (): eff loaded integer :=
  let strong xl: pointer =
    create(Ivalignof('unsigned long long'), 'unsigned long long') in
  let strong s: pointer =
    create(Ivalignof('struct __cerbty_unnamed_tag_653'), 'struct __cerbty_unnamed_tag_653') in
  let strong pxg: pointer =
    create(Ivalignof('unsigned long long*'), 'unsigned long long*') in
  let strong pxl: pointer =
    create(Ivalignof('unsigned long long*'), 'unsigned long long*') in
  let strong ps: pointer =
    create(Ivalignof('struct __cerbty_unnamed_tag_653*'), 'struct __cerbty_unnamed_tag_653*') in
  let strong psu: pointer =
    create(Ivalignof('unsigned long long*'), 'unsigned long long*') in
  let strong pm: pointer =
    create(Ivalignof('unsigned long long*'), 'unsigned long long*') in
  let strong a_674: loaded integer =
    {- §6.5#2 -}
    bound(pure(Specified(6))) in
  store('unsigned long long', xl, conv_loaded_int('unsigned long long', a_674)) ;
  let strong a_675: loaded struct a_653 =
    {- §6.5#2 -}
    bound(
      let weak (a_677: loaded integer, a_676: loaded integer) =
        {- §6.7.9#23 -}
        unseq(pure(Specified(7)), pure(Specified(65))) in
      pure(
        Specified((struct __cerbty_unnamed_tag_653){.c= conv_loaded_int('char',
        a_676), .u= conv_loaded_int('unsigned long long', a_677)})
      )
    ) in
  store('struct __cerbty_unnamed_tag_653', s, a_675) ;
  let strong a_678: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.2#3, sentence 5 -}
      let weak a_679: pointer = pure(xg) in
      pure(Specified(a_679))
    ) in
  store('unsigned long long*', pxg, a_678) ;
  {- §6.5#2 -}
  bound(
    {- §6.5.2.2#10, sentence 1 -}
    let strong ((a_680: loaded pointer,
    (a_681: ctype, a_682: [ctype], a_683: boolean, a_684: boolean)),
    a_686: loaded pointer) =
      {- §6.5.2.2#4, sentence 2 -}
      unseq(
        let strong a_673: loaded pointer = pure(Specified(Cfunction(f))) in
        pure((a_673, cfunction(a_673)))
      ,
        let weak a_687: pointer = pure(pxg) in
        load('unsigned long long*', a_687)
      ) in
    if not(params_length(a_682) = 1) then
      pure(undef(<<UB038_number_of_args>>))
    else
      if a_683 \/ not(are_compatible ('void', a_681)) then
        pure(undef(<<UB041_function_not_compatible>>))
      else
        let strong a_685: pointer =
          let a_688: ctype = params_nth(a_682, 0) in
          if not(are_compatible ('unsigned long long*', a_688)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            {- §6.5.2.2#7, sentence 1 -}
            let weak a_689: pointer = create(Ivalignof(a_688), a_688) in
            let weak _: unit = store(a_688, a_689, a_686) in
            pure(a_689) in
        let strong a_690: unit =
          ccall('void (*) (unsigned long long*)', a_680, a_685) in
        kill('unsigned long_long*', a_685) ;
        pure(a_690)
  ) ;
  pure(Unit) ;
  let strong a_691: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.2#3, sentence 5 -}
      let weak a_692: pointer = pure(xl) in
      pure(Specified(a_692))
    ) in
  store('unsigned long long*', pxl, a_691) ;
  {- §6.5#2 -}
  bound(
    {- §6.5.2.2#10, sentence 1 -}
    let strong ((a_693: loaded pointer,
    (a_694: ctype, a_695: [ctype], a_696: boolean, a_697: boolean)),
    a_699: loaded pointer) =
      {- §6.5.2.2#4, sentence 2 -}
      unseq(
        let strong a_672: loaded pointer = pure(Specified(Cfunction(f))) in
        pure((a_672, cfunction(a_672)))
      ,
        let weak a_700: pointer = pure(pxl) in
        load('unsigned long long*', a_700)
      ) in
    if not(params_length(a_695) = 1) then
      pure(undef(<<UB038_number_of_args>>))
    else
      if a_696 \/ not(are_compatible ('void', a_694)) then
        pure(undef(<<UB041_function_not_compatible>>))
      else
        let strong a_698: pointer =
          let a_701: ctype = params_nth(a_695, 0) in
          if not(are_compatible ('unsigned long long*', a_701)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            {- §6.5.2.2#7, sentence 1 -}
            let weak a_702: pointer = create(Ivalignof(a_701), a_701) in
            let weak _: unit = store(a_701, a_702, a_699) in
            pure(a_702) in
        let strong a_703: unit =
          ccall('void (*) (unsigned long long*)', a_693, a_698) in
        kill('unsigned long_long*', a_698) ;
        pure(a_703)
  ) ;
  pure(Unit) ;
  let strong a_704: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.2#3, sentence 5 -}
      let weak a_705: pointer = pure(s) in
      pure(Specified(a_705))
    ) in
  store('struct __cerbty_unnamed_tag_653*', ps, a_704) ;
  let strong a_706: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.2#3, sentence 5 -}
      let weak a_707: pointer =
        {- §6.5.2.3#4, sentence 2 -}
        let strong a_708: loaded pointer =
          let weak a_712: pointer = pure(ps) in
          load('struct __cerbty_unnamed_tag_653*', a_712) in
        let Specified(a_709: pointer) = a_708 in
        let weak a_710: boolean =
          memop(PtrValidForDeref, 'struct __cerbty_unnamed_tag_653', a_709) in
        pure(
          if a_710 then
            member_shift(a_709, __cerbty_unnamed_tag_653, .u)
          else
            undef(<<UB043_indirection_invalid_value>>)
        ) in
      pure(Specified(a_707))
    ) in
  store('unsigned long long*', psu, a_706) ;
  {- §6.5#2 -}
  bound(
    {- §6.5.2.2#10, sentence 1 -}
    let strong ((a_713: loaded pointer,
    (a_714: ctype, a_715: [ctype], a_716: boolean, a_717: boolean)),
    a_719: loaded pointer) =
      {- §6.5.2.2#4, sentence 2 -}
      unseq(
        let strong a_671: loaded pointer = pure(Specified(Cfunction(f))) in
        pure((a_671, cfunction(a_671)))
      ,
        let weak a_720: pointer = pure(psu) in
        load('unsigned long long*', a_720)
      ) in
    if not(params_length(a_715) = 1) then
      pure(undef(<<UB038_number_of_args>>))
    else
      if a_716 \/ not(are_compatible ('void', a_714)) then
        pure(undef(<<UB041_function_not_compatible>>))
      else
        let strong a_718: pointer =
          let a_721: ctype = params_nth(a_715, 0) in
          if not(are_compatible ('unsigned long long*', a_721)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            {- §6.5.2.2#7, sentence 1 -}
            let weak a_722: pointer = create(Ivalignof(a_721), a_721) in
            let weak _: unit = store(a_721, a_722, a_719) in
            pure(a_722) in
        let strong a_723: unit =
          ccall('void (*) (unsigned long long*)', a_713, a_718) in
        kill('unsigned long_long*', a_718) ;
        pure(a_723)
  ) ;
  pure(Unit) ;
  let strong a_724: loaded pointer =
    {- §6.5#2 -}
    bound(
      {- §6.5.4 -}
      let weak a_725: loaded pointer =
        {- §6.5.2.2#10, sentence 1 -}
        let strong ((a_728: loaded pointer,
        (a_729: ctype, a_730: [ctype], a_731: boolean, a_732: boolean)),
        a_734: loaded integer) =
          {- §6.5.2.2#4, sentence 2 -}
          unseq(
            let strong a_727: loaded pointer =
              pure(Specified(Cfunction(malloc_proxy))) in
            pure((a_727, cfunction(a_727)))
          ,
            pure(Specified(Ivsizeof('unsigned long long')))
          ) in
        if not(params_length(a_730) = 1) then
          pure(undef(<<UB038_number_of_args>>))
        else
          if a_731 \/ not(are_compatible ('void*', a_729)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            let strong a_733: pointer =
              let a_735: ctype = params_nth(a_730, 0) in
              if not(are_compatible ('size_t', a_735)) then
                pure(undef(<<UB041_function_not_compatible>>))
              else
                {- §6.5.2.2#7, sentence 1 -}
                let weak a_736: pointer = create(Ivalignof(a_735), a_735) in
                let weak _: unit =
                  store(a_735, a_736, conv_loaded_int(a_735, a_734)) in
                pure(a_736) in
            let strong a_737: loaded pointer =
              ccall('void* (*) (size_t)', a_728, a_733) in
            kill('size_t', a_733) ;
            pure(a_737) in
      let Specified(a_726: pointer) = a_725 in
      let weak a_738: boolean =
        memop(PtrWellAligned, 'unsigned long long', a_726) in
      pure(
        Specified(if a_738 then
          a_726
        else
          undef(<<UB025_misaligned_pointer_conversion>>))
      )
    ) in
  store('unsigned long long*', pm, a_724) ;
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_670: pointer, a_743: loaded integer) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.3.2 -}
          let weak a_739: loaded pointer =
            let weak a_742: pointer = pure(pm) in
            load('unsigned long long*', a_742) in
          let Specified(a_740: pointer) = a_739 in
          let weak a_741: boolean =
            memop(PtrValidForDeref, 'unsigned long long', a_740) in
          pure(
            if a_741 then
              a_740
            else
              undef(<<UB043_indirection_invalid_value>>)
          )
        ,
          pure(Specified(8))
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('unsigned long long', a_670,
        conv_loaded_int('unsigned long long', a_743))) in
      pure(conv_loaded_int('unsigned long long', a_743))
    ) in
  pure(Unit) ;
  {- §6.5#2 -}
  bound(
    {- §6.5.2.2#10, sentence 1 -}
    let strong ((a_744: loaded pointer,
    (a_745: ctype, a_746: [ctype], a_747: boolean, a_748: boolean)),
    a_750: loaded pointer) =
      {- §6.5.2.2#4, sentence 2 -}
      unseq(
        let strong a_669: loaded pointer = pure(Specified(Cfunction(f))) in
        pure((a_669, cfunction(a_669)))
      ,
        let weak a_751: pointer = pure(pm) in
        load('unsigned long long*', a_751)
      ) in
    if not(params_length(a_746) = 1) then
      pure(undef(<<UB038_number_of_args>>))
    else
      if a_747 \/ not(are_compatible ('void', a_745)) then
        pure(undef(<<UB041_function_not_compatible>>))
      else
        let strong a_749: pointer =
          let a_752: ctype = params_nth(a_746, 0) in
          if not(are_compatible ('unsigned long long*', a_752)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            {- §6.5.2.2#7, sentence 1 -}
            let weak a_753: pointer = create(Ivalignof(a_752), a_752) in
            let weak _: unit = store(a_752, a_753, a_750) in
            pure(a_753) in
        let strong a_754: unit =
          ccall('void (*) (unsigned long long*)', a_744, a_749) in
        kill('unsigned long_long*', a_749) ;
        pure(a_754)
  ) ;
  pure(Unit) ;
  kill('unsigned long_long', xl) ;
  kill('struct __cerbty_unnamed_tag_653', s) ;
  kill('unsigned long_long*', pxg) ;
  kill('unsigned long_long*', pxl) ;
  kill('struct __cerbty_unnamed_tag_653*', ps) ;
  kill('unsigned long_long*', psu) ;
  kill('unsigned long_long*', pm) ;
  pure(Unit) ;
  save ret_668: loaded integer (a_755: loaded integer:= Specified(0)) in
    pure(a_755)

