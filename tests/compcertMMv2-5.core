-- Aggregates
def union _G_fpos64_t :=
  __opaque: 'char[16]'
  __lldata: 'signed long_long'
  __align: 'double'

def union __cerbty_unnamed_tag_649 :=
  f: 'float'
  i: 'unsigned int'

-- Globals
glob a_682: pointer [ail_ctype = 'char[6]'] :=
  create_readonly(Ivalignof('char[6]'), 'char[6]', Specified(Array(Specified(conv_int('char',
  103)), Specified(conv_int('char', 61)), Specified(conv_int('char', 37)),
  Specified(conv_int('char', 102)), Specified(conv_int('char', 10)),
  Specified(0))))

-- Fun map
proc fabs_single (x: pointer): eff loaded floating :=
  let strong u: pointer =
    create(Ivalignof('union __cerbty_unnamed_tag_649'), 'union __cerbty_unnamed_tag_649') in
  {- §6.2.4#6 -}
  store('union __cerbty_unnamed_tag_649', u,
  Unspecified('union __cerbty_unnamed_tag_649')) ;
  let strong _: loaded floating =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_694: pointer, a_697: loaded floating) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.2.3#3, sentence 2 -}
          let strong a_695: pointer = pure(u) in
          memop(PtrArrayShift[__cerbty_unnamed_tag_649, f], a_695)
        ,
          let weak a_696: pointer = pure(x) in
          load('float', a_696)
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('float', a_694, a_697)) in
      pure(a_697)
    ) in
  pure(Unit) ;
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_693: pointer, a_705: loaded integer) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          {- §6.5.2.3#3, sentence 2 -}
          let strong a_698: pointer = pure(u) in
          memop(PtrArrayShift[__cerbty_unnamed_tag_649, i], a_698)
        ,
          {- §6.5.10 -}
          let weak (a_699: loaded integer, a_700: loaded integer) =
            unseq(
              let weak a_704: pointer =
                {- §6.5.2.3#3, sentence 2 -}
                let strong a_703: pointer = pure(u) in
                memop(PtrArrayShift[__cerbty_unnamed_tag_649, i], a_703) in
              load('unsigned int', a_704)
            ,
              pure(Specified(2147483647))
            ) in
          pure(
            let (Specified(a_701: integer), Specified(a_702: integer)) =
              (a_699, a_700) in
            Specified(IvAND('unsigned int', conv_int('unsigned int', a_701),
            conv_int('unsigned int', a_702)))
          )
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('unsigned int', a_693,
        conv_loaded_int('unsigned int', a_705))) in
      pure(conv_loaded_int('unsigned int', a_705))
    ) in
  pure(Unit) ;
  let strong a_708: loaded floating =
    {- §6.5#2 -}
    bound(
      let weak a_707: pointer =
        {- §6.5.2.3#3, sentence 2 -}
        let strong a_706: pointer = pure(u) in
        memop(PtrArrayShift[__cerbty_unnamed_tag_649, f], a_706) in
      load('float', a_707)
    ) in
  kill('union __cerbty_unnamed_tag_649', u) ;
  run ret_692(a_708) ;
  kill('union __cerbty_unnamed_tag_649', u) ;
  pure(Unit) ;
  save ret_692: loaded floating (a_709: loaded floating:= undef(<<UB088_reached_end_of_function>>)) in
    pure(a_709)

proc main (): eff loaded integer :=
  let strong f: pointer = create(Ivalignof('float'), 'float') in
  let strong g: pointer = create(Ivalignof('float'), 'float') in
  let strong a_658: loaded floating =
    {- §6.5#2 -}
    bound(
      {- §6.5.3.3#3 -}
      let weak a_660: loaded floating = pure(Specified(1.)) in
      pure(
        let Specified(a_659: floating) = a_660 in
        Specified(0. - a_659)
      )
    ) in
  store('float', f, a_658) ;
  {- §6.2.4#6 -}
  store('float', g, Unspecified('float')) ;
  let strong _: loaded floating =
    {- §6.5#2 -}
    bound(
      {- §6.5.16#3, sentence 4 -}
      let weak (a_657: pointer, a_673: loaded floating) =
        {- §6.5.16#3, sentence 5 -}
        unseq(
          pure(g)
        ,
          {- §6.5.2.2#10, sentence 1 -}
          let strong ((a_662: loaded pointer,
          (a_663: ctype, a_664: [ctype], a_665: boolean, a_666: boolean)),
          a_668: loaded floating) =
            {- §6.5.2.2#4, sentence 2 -}
            unseq(
              let strong a_661: loaded pointer =
                pure(Specified(Cfunction(fabs_single))) in
              pure((a_661, cfunction(a_661)))
            ,
              let weak a_669: pointer = pure(f) in
              load('float', a_669)
            ) in
          if not(params_length(a_664) = 1) then
            pure(undef(<<UB038_number_of_args>>))
          else
            if a_665 \/ not(are_compatible ('float', a_663)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              let strong a_667: pointer =
                let a_670: ctype = params_nth(a_664, 0) in
                if not(are_compatible ('float', a_670)) then
                  pure(undef(<<UB041_function_not_compatible>>))
                else
                  {- §6.5.2.2#7, sentence 1 -}
                  let weak a_671: pointer = create(Ivalignof(a_670), a_670) in
                  let weak _: unit = store(a_670, a_671, a_668) in
                  pure(a_671) in
              let strong a_672: loaded floating =
                ccall('float (*) (float)', a_662, a_667) in
              kill('float', a_667) ;
              pure(a_672)
        ) in
      let weak _: unit =
        {- §6.5.16.1#2, store -}
        neg(store('float', a_657, a_673)) in
      pure(a_673)
    ) in
  pure(Unit) ;
  let strong _: loaded integer =
    {- §6.5#2 -}
    bound(
      {- §6.5.2.2#10, sentence 1 -}
      let strong ((a_674: loaded pointer,
      (a_675: ctype, a_676: [ctype], a_677: boolean, a_678: boolean)),
      a_681: loaded pointer, a_685: loaded floating) =
        {- §6.5.2.2#4, sentence 2 -}
        unseq(
          let strong a_656: loaded pointer =
            pure(Specified(Cfunction(printf_proxy))) in
          pure((a_656, cfunction(a_656)))
        ,
          let weak a_683: pointer = pure(a_682) in
          {- §6.3.2.1#3 -}
          let weak a_684: pointer = memop(PtrArrayShift, a_683, 'char', 0) in
          pure(Specified(a_684))
        ,
          let weak a_686: pointer = pure(g) in
          load('float', a_686)
        ) in
      if not(params_length(a_676) <= 2) then
        pure(undef(<<UB038_number_of_args>>))
      else
        if not(a_677) \/ not(are_compatible ('signed int', a_675)) then
          pure(undef(<<UB041_function_not_compatible>>))
        else
          let strong a_680: pointer =
            let a_687: ctype = params_nth(a_676, 0) in
            if not(are_compatible ('const char*', a_687)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              {- §6.5.2.2#7, sentence 1 -}
              let weak a_688: pointer = create(Ivalignof(a_687), a_687) in
              let weak _: unit = store(a_687, a_688, a_681) in
              pure(a_688) in
          let strong a_679: pointer =
            {- §6.5.2.2#7, sentences 2 and 3 -}
            pcall(create_and_store, 'double', a_685) in
          let strong a_689: loaded integer =
            ccall('signed int (*) (const char*restrict , ...)', a_674, a_680,
            [('double', a_679)]) in
          let strong _: (unit,unit) =
            unseq(kill('char*', a_680), kill('double', a_679)) in
          pure(a_689)
    ) in
  pure(Unit) ;
  kill('float', f) ;
  kill('float', g) ;
  pure(Unit) ;
  save ret_655: loaded integer (a_690: loaded integer:= Specified(0)) in
    pure(a_690)

